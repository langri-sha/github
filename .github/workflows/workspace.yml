name: Workspace

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  check:
    permissions:
      contents: write
    uses: ./.github/workflows/check.yml
    with:
      prettier: true
      projen: true
      typescript: true
    secrets: inherit
